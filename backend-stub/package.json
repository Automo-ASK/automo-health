{
  "name": "@automo/backend-stub",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Shared dev stub of Koded's booking API. Implements docs/contracts/booking-api.md with in-memory data so the channels unblock. Retired once backend/ is live.",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  }
}
